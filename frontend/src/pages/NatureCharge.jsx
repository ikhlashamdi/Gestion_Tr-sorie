import Layout from '../components/common/Layout'
import NatureChargeList from '../components/NatureDeCharge/NatureChargeList'

const NatureCharge = () => {
    return (
        <Layout>
            <NatureChargeList className='mt-[var(--nav-height)]' />
        </Layout>
    )
}

export default NatureCharge
